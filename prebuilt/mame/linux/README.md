# Prebuilt MAME packages (Linux)

- Version: 0.242+dfsg.1-1 (Ubuntu jammy `amd64` build)
- Source: https://mirrors.edge.kernel.org/ubuntu/pool/universe/m/mame/
- Files included:
  - `mame_0.242+dfsg.1-1_amd64.deb` (binary)
  - `mame-data_0.242+dfsg.1-1_all.deb` (hash/data/artwork)
- License: MAME license (see upstream package metadata)

Update instructions:
1. Download newer `.deb` files from the upstream URL above.
2. Replace the existing files in this directory with the new versions.
3. Keep the workflow in sync if the file names or version folder changes.
